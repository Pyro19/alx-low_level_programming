#include "main.h"
#include <stdio.h>
/**
 * main - print its name
 * @argv: array pointing to argument
 * @argc: argument count
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);
}

