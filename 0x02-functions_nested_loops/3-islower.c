#include "main.h"
/**
 * _islower - checks for lowercase character
 * @c: is the int that will be use for thr argument of the function
 *
 * Return: 0
 */
int_islowe(int c)
{
	if ( c>= 'a' && c <= 'z')
	{
		return (1);
	}
	else
		return (0)
}
