#include "main.h"
#include <stdio.h>
/**
 * _abs - function that compute the absolute value of an integer
 * @c: integer use for the argument of the function
 *
 * Return: 0
 */
int _abs(int c)
{
	if (c > 0 || c == 0)
	{
		return (c);
	}
	else
	retun (c* -1);
}
