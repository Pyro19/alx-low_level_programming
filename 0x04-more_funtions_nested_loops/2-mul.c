#include "main.h"
/**
 * mul - multiplies two integers
 * @x: The first number to multiply
 * @y: The second numer to multiply
 * Return: The result of multiplication
 */
int mul(int x, int y)
{
	return (x * y);
}
